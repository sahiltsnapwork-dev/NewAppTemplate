# API Execution Trace — search_stocks

**Generated by:** flutter-rn-orchestrator  
**Feature:** search_stocks  
**Total APIs:** 2

---

## API 1 — Search Stocks

| Field | Value |
|---|---|
| Method | `GET` |
| Base URL | `process.env.API_BASE_URL` (configured in `.env`) |
| Path | `{API_BASE_URL}/content-services/search/query?q={query}` |
| Resolved Example | `https://api.example.com/content-services/search/query?q=INFY` |
| Headers | `Authorization: Bearer {token}` (injected by interceptor) |
| Query Params | `q` — URL-encoded search query string |
| Request Body | None (GET) |
| Repository Method | `SearchStocksRepositoryImpl.searchStocks(query: string)` |
| Data Source | `SearchStocksApiDataSource.searchStocks(query)` |
| Thunk | `searchStocksThunk` |

### Response Shape
```json
{
  "data": {
    "nsebsescript": [{ "companyName": "", "symbol": "", "exchange": "", ... }],
    "futurescrip":  [{ "companyName": "", "symbol": "", "expiryDate": "", "instrumentId": "", ... }],
    "nsefoscrip":   [{ "companyName": "", "symbol": "", "strikePrice": 0, "optionType": "", ... }],
    "etfscrip":     [{ "companyName": "", "symbol": "", ... }],
    "mfscheme":     [{ "schemeDesc": "", "schemeID": "", "navValue": 0, "navDate": "" }]
  }
}
```

### Status Code Handling
| Status | Behaviour |
|---|---|
| 200 | Parse and map all instrument categories |
| Other | Throw — caught by Redux thunk, sets `searchStatus: "failed"` |

---

## API 2 — Fetch Trending Stocks

| Field | Value |
|---|---|
| Method | `GET` |
| Base URL | `process.env.API_BASE_URL` |
| Path | `{API_BASE_URL}/content-services/trending/stocks` |
| Resolved Example | `https://api.example.com/content-services/trending/stocks` |
| Headers | `Authorization: Bearer {token}` |
| Query Params | None |
| Request Body | None (GET) |
| Repository Method | `SearchStocksRepositoryImpl.fetchTrendingStocks()` |
| Data Source | `SearchStocksApiDataSource.fetchTrendingStocks()` |
| Thunk | `fetchTrendingStocksThunk` |

### Response Shape
```json
{
  "data": [
    {
      "instrumentId": "",
      "hslcode": "",
      "name": "",
      "nsesymbol": "",
      "nselistedflag": "Y",
      "isin": ""
    }
  ]
}
```

### Status Code Handling
| Status | Behaviour |
|---|---|
| 200 | Map to `StockEntity[]` using `mapTrendingDatumToStockEntity` |
| Other | Throw — sets `trendingStatus: "failed"` |

---

## Source: Flutter original endpoints

- `AppConstant.SEARCH_URL + query` → `GET {baseUrl}/content-services/search/query?q={query}`
- `AppConstant.SEARCH_TRENDING_URL` → `GET {baseUrl}/content-services/trending/stocks`

> **ACTION REQUIRED:** Confirm exact path suffixes with your backend team. The paths above are inferred from the Flutter `SEARCH_URL` and `SEARCH_TRENDING_URL` constants. Set `API_BASE_URL` in `.env` before running the app.
